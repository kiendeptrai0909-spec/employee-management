[phases.setup]
nixPkgs = ["jdk17"]

[phases.build]
cmds = ["cd backend && chmod +x mvnw && ./mvnw clean package -DskipTests"]

[start]
cmd = "java -jar backend/target/employeesystem-0.0.1-SNAPSHOT.jar"
